// Including Libraries
#include <stdio.h>
#include <cuda_runtime.h>

// Defining the CUDA Kernel
// __global__ void vectorAdd(int *a, int *b, int *c, int n) {
//     int i = threadIdx.x;
//     if (i < n) {
//         c[i] = a[i] + b[i];
//     }
// }


// Main Function
int main() {
    return 0;
}
